services:
  db:
    image: mysql:8
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: kelurahan_bot
      MYSQL_USER: kelurahan
      MYSQL_PASSWORD: kelurahan
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3316:3306"
    volumes:
      - db_data:/var/lib/mysql

  adminer:
    image: adminer
    restart: unless-stopped
    ports:
      - "8091:8080"
    depends_on:
      - db

  phpmyadmin:
    image: phpmyadmin
    restart: unless-stopped
    environment:
      PMA_HOST: db
      PMA_USER: kelurahan
      PMA_PASSWORD: kelurahan
    ports:
      - "8092:80"
    depends_on:
      - db

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: ["redis-server", "--save", "60", "1"]

volumes:
  db_data:
  redis_data:
